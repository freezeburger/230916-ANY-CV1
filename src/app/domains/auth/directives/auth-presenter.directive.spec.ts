import { AuthPresenterDirective } from './auth-presenter.directive';

describe('AuthPresenterDirective', () => {
  it('should create an instance', () => {
    const directive = new AuthPresenterDirective();
    expect(directive).toBeTruthy();
  });
});
