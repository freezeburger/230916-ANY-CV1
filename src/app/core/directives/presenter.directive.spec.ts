import { PresenterDirective } from './presenter.directive';

describe('PresenterDirective', () => {
  it('should create an instance', () => {
    const directive = new PresenterDirective();
    expect(directive).toBeTruthy();
  });
});
